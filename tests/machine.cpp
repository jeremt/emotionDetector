
#include "testy.hpp"

Suite("Machine learning", {
  describe("Training",
    it("Should train the machine with several data sets",
      test(true);
    ),
    it("Should check performances",
      test(true);
    )
  )
  describe("Smile detection",
    it("Should detect smiles from easy photos",
    ),
    it("Should detect smiles from hard and dark photos",
    )
  )
})
